## Alair's Website

Personal site built with [Next.js](https://nextjs.org), TypeScript, and `pnpm`.

### Setup

```bash
pnpm install
pnpm dev
```

Then open `http://localhost:3000`.

### Production build

```bash
pnpm build
pnpm start
```
